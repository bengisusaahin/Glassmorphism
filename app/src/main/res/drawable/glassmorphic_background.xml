<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Border -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:color="#2B0A0A"
                android:width="2dp"
                android:dashWidth="@dimen/cardview_default_radius"
                android:dashGap="@dimen/material_emphasis_medium"
                /> <!-- Border width -->
            <corners android:radius="16dp" />
        </shape>
    </item>
    <!-- Background (with glassmorphic effect) -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="#80710E0E" /> <!-- Background color -->
            <corners android:radius="16dp" />
            <gradient android:type="linear"
                android:startColor="#6A1717"
                android:endColor="@color/cardview_light_background"
                android:angle="0"/>
        </shape>
    </item>
</layer-list>
